// const moviesRouter = require('express').Router();
// const {
//   getUser, getUsers, getUserInfo, updateUser, updateAvatar,
// } = require('../controllers/movies');

// const { validateMoviesPost, validateMovieId } = require('../middlewares/celebrate');

// moviesRouter.get('/', getMovies);
// moviesRouter.post('/', validateMoviesPost, createMovie);
// moviesRouter.delete('_id', validateMovieId, deleteMovie);

// module.exports = moviesRouter;
